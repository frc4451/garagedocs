---
title: "Java Setup"
lessonId: java-setup
section: java
group: java-basics
groupLabel: "Java Basics"
groupOrder: 1
order: 2
difficulty: beginner
duration: 30 min
description: "Install Eclipse Temurin Java 25 and set up VS Code for the local workflow used by the Practice Assignments."
durationFixed: true
---

### Why work locally

<ReferenceImage
  src="/media/java/jvm-architecture.svg"
  alt="Diagram showing Java source compiled to bytecode and run by a Java virtual machine on different operating systems"
  caption="The JDK compiles Java source into bytecode. A Java Virtual Machine then runs that bytecode on the local operating system."
  sourceUrl="https://commons.wikimedia.org/wiki/File:Java_virtual_machine_architecture.svg"
  sourceLabel="Wikimedia Commons, CC0"
/>

Most lesson examples and the Practice checks run in the browser. The runner needs an internet connection; local Java works offline after installation.

A local setup gives you Java 25, file-based program input, and a debugger. The browser uses Java 17, while **Java 25 is the standard** for local Java lessons. WPILib robot projects use the JDK bundled with WPILib. The Kit Bot section assumes you have completed this local setup.

If you cannot install software on the computer you use (a school machine with locked-down permissions is common), use the browser for supported examples and checks. Ask a mentor for an approved computer for debugger, multi-file, or file-system tasks.

### Installing a JDK

You need a Java Development Kit. Install **Java 25**, the version every JavaDoc link on this site points at. Download the free [Eclipse Temurin JDK 25](https://adoptium.net/temurin/releases/?version=25). Robot code is different: the WPILib installer in [R00](/kit-bot/r00-wpilib-setup#what-wpilib-installs) brings its own JDK, and you never point it at this one.

<StepsBox title="Install steps">

1. Open the [Eclipse Temurin JDK 25 downloads](https://adoptium.net/temurin/releases/?version=25). Select your operating system and architecture, then download the **JDK** installer.
2. Run the installer. On Windows, accept the option to add Java to `PATH` if offered.
3. Open a new terminal and confirm the install:

```
javac --version
java --version
```

Both commands should report version 25 for this local setup. If `javac` is missing, check the JDK installation and PATH; a working `java` command alone does not show which JDK the terminal found.

</StepsBox>

### Compiling and running

The first assignments use single-file programs; some later tasks use several files. Keep the class name and the file name matching: `HelloTeam` lives in `HelloTeam.java`, and `java HelloTeam` is how you run it.

```java
// File: Main.java
import java.io.*;

public class Main {
    public static void main(String[] args) throws IOException {
        BufferedReader in = new BufferedReader(new InputStreamReader(System.in));
        int n = Integer.parseInt(in.readLine().trim());
        System.out.println(n * 2);
    }
}
```

Compile and run:

```
javac Main.java
java Main
```

Newer JDKs also let you skip the compile step for single-file programs, which is convenient while iterating:

```
java Main.java
```

### Testing against sample input

Several Practice Assignments read from standard input. A04 asks for five values, A24 takes a whole session of commands. Put that input in a file instead of typing it every run.

Save the sample as `in.txt`. Run the appropriate command from the folder containing your compiled program.

<CodeTabs>
<CodeTab label="PowerShell">

```powershell
Get-Content -LiteralPath in.txt | java Main
```

</CodeTab>
<CodeTab label="Terminal">

```bash
java Main < in.txt
```

</CodeTab>
</CodeTabs>

PowerShell does not support the shell input-redirection operator `<`. The pipeline above supplies the text lines as standard input.

To save the output for comparison:

<CodeTabs>
<CodeTab label="PowerShell">

```powershell
Get-Content -LiteralPath in.txt | java Main > out.txt
```

</CodeTab>
<CodeTab label="Terminal">

```bash
java Main < in.txt > out.txt
```

</CodeTab>
</CodeTabs>

Save the expected output as `expected.txt`. In VS Code, select `expected.txt` in Explorer, choose **Select for Compare**, then select `out.txt` and choose **Compare with Selected**. Check line order, spaces, and extra prompts. The browser's **Check** button applies the assignment's automated comparisons.


<RulesBox title="A workflow that holds up">

- Keep one folder per track, one subfolder per assignment.
- In each assignment folder: the `.java` file the assignment names, `in.txt`, and `expected.txt` for what it should print.
- Before you call an assignment done, run the sample once more. It catches the case where you fixed one thing and broke another.

</RulesBox>

### Setting up VS Code

The command line above is enough to check the installation. Use **Visual Studio Code (VS Code)** for every local Java lesson and assignment. It is the editor FRC 4451 also uses for robot code.

Install the free [Extension Pack for Java](https://marketplace.visualstudio.com/items?itemName=vscjava.vscode-java-pack). It catches many typos as you type, completes method names, and provides a debugger. The WPILib copy of VS Code already includes Java support.

The [VS Code lessons](/tools/vscode-tour#open-the-folder-not-the-file) in the Tools section explain the editor in more detail. For these lessons, **open the folder** that contains your `.java` files. Opening only one file prevents the Java tools from seeing the project.

Use the *Run* link above `main` to start the program. Program output and keyboard input both appear in the [integrated terminal](/tools/vscode-tour#the-integrated-terminal).

<RulesBox title="Feeding sample input in VS Code">

The *Run* link reads from the terminal, so type the sample by hand or paste it. To feed a file instead, use the command for your operating system above. `` Ctrl+` `` opens it.

</RulesBox>

### If you cannot install anything

Use the browser editor and its input box for supported examples and assignment checks. It runs Java 17 and requires internet. Some assignment pages include additional local tasks, so coordinate access to an approved computer with a mentor.

<ExerciseBox title="Verify your setup" description="Confirm the whole loop works before the first assignment asks for it:">

1. Create a folder with `Main.java` containing the program above.
2. Create `in.txt` containing a single line with the number `21`.
3. Run the program with input redirected. Confirm it prints `42`.
4. Change `in.txt` to `-5` and run again without editing the Java file. Confirm it prints `-10`.

Step 4 is the point of the exercise: you changed the test case without touching code or retyping input.

</ExerciseBox>

<LinkGrid
  title="Related"
  section="java"
  links={[
    {"label":"Variables","id":"java-variables"},
    {"label":"Packages and Project Layout","id":"java-packages-and-project-layout"},
    {"label":"A01: Hello, Team","url":"/java/assignments/a01-hello-team"}
  ]}
/>

<Sources items={[
  {"url": "https://learn.microsoft.com/en-us/powershell/module/microsoft.powershell.core/about/about_redirection?view=powershell-7.5", "label": "PowerShell redirection", "note": "Input redirection differences and piping text to native programs."},
  {"url": "https://adoptium.net/temurin/releases/?version=25", "label": "Eclipse Temurin JDK 25", "note": "Local Java 25 downloads for Windows, macOS, and Linux."},
  {"url": "https://docs.oracle.com/en/java/javase/25/docs/specs/man/javac.html", "label": "javac reference", "note": "Compiling."},
  {"url": "https://docs.oracle.com/en/java/javase/25/docs/specs/man/java.html", "label": "java launcher reference", "note": "Running, including single-file source launch."},
  {"url": "https://marketplace.visualstudio.com/items?itemName=vscjava.vscode-java-pack", "label": "Extension Pack for Java", "note": "The VS Code extensions."},
  {"source": "vscode", "path": "java/java-tutorial", "note": "Java in VS Code."},
  {"source": "vscode", "path": "terminal/basics", "note": "The integrated terminal."},
  {"source": "java-api", "path": "java.base/java/lang/System.html", "note": "<code>System</code> API reference."}
]} />

<LinkGrid
  title="Downloads"
  links={[
    {"label":"Eclipse Temurin website","url":"https://adoptium.net/"},
    {"label":"Download Eclipse Temurin JDK 25","url":"https://adoptium.net/temurin/releases/?version=25"},
    {"label":"VS Code Java Pack","url":"https://marketplace.visualstudio.com/items?itemName=vscjava.vscode-java-pack"}
  ]}
/>

<LinkGrid title="Next lesson" section="java" links={[{"label": "Printing", "id": "java-printing"}]} />
