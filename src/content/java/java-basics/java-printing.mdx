---
title: "Printing"
lessonId: java-printing
section: java
group: java-basics
groupLabel: "Java Basics"
groupOrder: 1
order: 3
difficulty: beginner
duration: 20 min
description: "System.out.println, the shape of a complete Java program, comments, and why printing is the first debugging tool you will use on a robot."
---

### Basic printing

`System.out.println()` prints a line of text to the console. Most of the programs in this track use it to show their results.

### Basic print example

```java
System.out.println("Hello world!");
System.out.println("Welcome to Java programming!");

// Output:
// Hello world!
// Welcome to Java programming!
```

### Program structure

The complete programs in this curriculum use a public class containing `public static void main(String[] args)`. Execution starts in that method. Use this form in local files so the examples also work with the Java 17 browser runner.

### Complete Java program

```java
public class MyProgram {
    public static void main(String[] args) {
        // Your code goes here
        System.out.println("Hello from Java!");
        System.out.println("This is my first program");
    }
}
```

### Key concepts

- **Argument:** The value supplied inside the parentheses, such as `"Hello"`
- **Semicolon:** Ends a statement such as a method call; braces around a class or method do not need one
- **Quotation marks:** Text must be enclosed in double quotes `""`
- **Case sensitivity:** `System` must be capitalized exactly

### Multiple lines and comments

```java
public class Example {
    public static void main(String[] args) {
        // Single-line comment
        System.out.println("First line");
        System.out.println("Second line");
        
        /* Multi-line comment
           for longer explanations */
        System.out.println("Third line");
    }
}
```

<RulesBox title="Printing best practices">

**Good Practices:**

- Use meaningful text in your print statements
- Add comments to explain your code
- Use VS Code suggestions to complete method names instead of guessing their spelling
- One command per line for readability

**Avoid:**

- Forgetting semicolons at the end of statements
- Misspelling `println` as `printin`
- Missing quotation marks around text
- Writing everything on one line

</RulesBox>

<JavaPlayground id="java-printing" />

<ExerciseBox title="Practice">

1. Print your name on one line and a subsystem name on the next.
2. Add a third println, predict where it appears, then run the program.
3. Remove one closing quote, read the compiler message, and restore it.

Predict each result, run the code, and explain any difference before continuing.

</ExerciseBox>

### Applications in FRC

In robot code, printing to the console is the quickest way to see a value while testing: no dashboard has to be configured first. The output appears in the Driver Station console and in the log.

### FRC console printing example

```java norun
System.out.println("********** Drive FF Characterization Results **********");
System.out.println("\tkS: " + formatter.format(kS));
System.out.println("\tkV: " + formatter.format(kV));
```

### Explanation

The example prints two motor-model values with labels. You do not need to understand `kS` or `kV` here; focus on how the labels make printed numbers identifiable. For values you want to watch continuously, or plot, a dashboard or AdvantageScope is the better tool; printing is for one-off checks.

<Sources items={[
  {"source": "csce145", "note": "The course whose structure this lesson follows."},
  {"source": "dev-java", "path": "getting-started/", "note": "The first program."},
  {"source": "java-api", "path": "java.base/java/lang/System.html", "note": "<code>System.out</code>."},
  {"source": "java-api", "path": "java.base/java/io/PrintStream.html", "note": "<code>println</code>, <code>print</code>, <code>printf</code>."},
  {"source": "jls", "path": "jls-3.html", "note": "Comments and lexical structure (chapter 3)."},
  {"source": "java-api", "path": "java.base/java/lang/String.html", "note": "<code>String</code> API reference."}
]} />


<LinkGrid title="Next lesson" section="java" links={[{"label": "Variables", "id": "java-variables"}]} />
