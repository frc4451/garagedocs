---
title: "For Loops"
lessonId: java-for-loops
section: java
group: java-loops
groupLabel: "Loops and Iteration"
groupOrder: 3
order: 2
difficulty: beginner
duration: 30 min
description: "The for loop for repeating a known number of times (initialization, condition, increment), with counting and accumulating examples."
---

### Introduction to for loops

A for loop repeats a block a known number of times. Its header holds the three parts of counting in one place: where the counter starts, when the loop stops, and how the counter changes each time.

<ReferenceImage
  src="/media/java/while-loop.svg"
  alt="Flowchart showing a loop condition, a repeated action when true, and an exit when false"
  caption="Every loop follows the same control-flow shape: test a condition, run the body while it is true, then exit when it becomes false."
  sourceUrl="https://commons.wikimedia.org/wiki/File:While-loop-diagram.svg"
  sourceLabel="Wikimedia Commons"
/>

### For loop structure

```java norun
// Basic for loop structure
for (initialization; condition; increment) {
    // Code to repeat
}

// Example: Print numbers 0 to 9
for (int i = 0; i < 10; i++) {
    System.out.println(i);
}

// Same logic as while loop:
// int i = 0;           // initialization
// while (i < 10) {     // condition
//     System.out.println(i);
//     i++;             // increment
// }
```

### For loop examples

```java
// Count with variables
int start = 3;
int end = 7;
for (int i = start; i < end; i++) {
    System.out.println("Count: " + i);
}
// Output: 3, 4, 5, 6

// Robot motor test - run each motor for 5 seconds
String[] motorNames = {"frontLeft", "frontRight", "backLeft", "backRight"};

for (int i = 0; i < motorNames.length; i++) {
    System.out.println("Testing motor: " + motorNames[i]);
    // setMotorPower(motorNames[i], 0.5);
    // wait(5000);  // 5 seconds
    // setMotorPower(motorNames[i], 0.0);
}
```

### Calculating with for loops

```java
// Calculate 4 * 3 as repeated addition (3 + 3 + 3 + 3)
int result = 0;

for (int i = 0; i < 4; i++) {
    result += 3;  // Add 3 each time
}

System.out.println("4 * 3 = " + result);  // 12

// Robot sensor average over 10 readings
int sensorSum = 0;
int readings = 10;

for (int i = 0; i < readings; i++) {
    // int sensorValue = readSensor();
    int sensorValue = (int)(Math.random() * 100);  // Simulate sensor
    sensorSum += sensorValue;
    System.out.println("Reading " + (i + 1) + ": " + sensorValue);
}

double average = (double) sensorSum / readings;
System.out.println("Average sensor value: " + average);
```

<RulesBox title="When to use a for loop">

- The number of repetitions is known before the loop starts
- Walking an array or collection by index
- Counting up or down by a fixed step
- A calculation that uses the counter, such as a running total

</RulesBox>

<JavaPlayground id="java-for-loops" />

<ExerciseBox title="Practice">

1. Print integers 1 through 5. Predict the first and last iteration.
2. Sum integers 1 through 5 and confirm 15.
3. Use an outer loop for rows 1 through 3 and an inner loop for columns 1 through 3. Print each row and column pair.

Predict each result, run the code, and explain any difference before continuing.

</ExerciseBox>

<Sources items={[
  {"source": "csce145", "note": "The course whose structure this lesson follows."},
  {"source": "java-tutorial", "path": "java/nutsandbolts/for.html", "note": "The for statement."},
  {"source": "dev-java", "path": "language-basics/controlling-flow/", "note": "Loops."},
  {"source": "jls", "path": "jls-14.html", "note": "The for statement (section 14.14)."},
  {"source": "java-api", "path": "java.base/java/lang/System.html", "note": "<code>System</code> API reference."},
  {"source": "java-api", "path": "java.base/java/lang/String.html", "note": "<code>String</code> API reference."},
  {"source": "java-api", "path": "java.base/java/lang/Math.html", "note": "<code>Math</code> API reference."},
  {"source": "java-api", "path": "java.base/java/lang/Thread.html", "note": "<code>Thread</code> API reference."},
  {"source": "java-api", "path": "java.base/java/lang/InterruptedException.html", "note": "<code>InterruptedException</code> API reference."},
  {"source": "java-api", "path": "java.base/java/util/Scanner.html", "note": "<code>Scanner</code> API reference."}
]} />

<LinkGrid title="Next lesson" section="java" links={[{"label": "Arrays", "id": "java-arrays"}]} />
