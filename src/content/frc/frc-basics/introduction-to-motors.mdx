---
title: "Introduction to Motors"
lessonId: introduction-to-motors
section: frc
group: frc-basics
groupLabel: "Robot Programming Basics"
groupOrder: 3
order: 1
difficulty: beginner
duration: 10 min
description: "Creating and commanding Talon FX and SPARK MAX motors from Robot.java, the lifecycle methods involved, and running more than one."
---

### How code reaches a motor

This page shows how to create CTRE Talon FX and REV SPARK MAX motor-controller objects in `Robot.java`, set their output from the robot lifecycle, reverse direction, and stop them.

Both vendors appear throughout this curriculum, in tabbed code samples so you can compare them. The Talon FX tab comes first because the examples in later lessons build on a Phoenix 6 robot (Talon FX controllers, a Pigeon 2 gyro, CANcoders). Learn the REV side too; plenty of teams and offseason projects run it.

### Robot.java lifecycle

**robotInit():** Runs once at startup. Create your motors here.

**teleopPeriodic():** Runs repeatedly during driver control. Update motor outputs here.

### CTRE: Talon FX

The Talon FX is a high-performance motor controller with a built-in encoder. It requires the CTRE Phoenix 6 library.

<ReferenceImage
  src="/media/hardware/talonfx.png"
  alt="CTRE Talon FX integrated brushless motor controller"
  caption="A Talon FX combines the motor, controller, and rotor sensor in one assembly. The Falcon 500, Kraken X60, and Kraken X44 use this controller family."
  sourceUrl="https://docs.wpilib.org/en/stable/docs/controls-overviews/control-system-hardware.html"
  sourceLabel="WPILib control-system hardware overview"
/>

### Talon FX Example

```java
package frc.robot;

import edu.wpi.first.wpilibj.TimedRobot;
import com.ctre.phoenix6.hardware.TalonFX;
import com.ctre.phoenix6.controls.DutyCycleOut;

public class Robot extends TimedRobot {
    // Create motor instance with CAN ID 1
    private TalonFX m_motor = new TalonFX(1);
    // Create control request for duty cycle output
    private DutyCycleOut m_output = new DutyCycleOut(0);
    
    @Override
    public void teleopPeriodic() {
        // Set motor to 50% speed forward (range: -1.0 to 1.0)
        m_motor.setControl(m_output.withOutput(0.5));
    }
}
```

### REV: SPARK MAX

The SPARK MAX supports both brushless (NEO) and brushed motors. It requires the REVLib library.

<ReferenceImage
  src="/media/hardware/spark-max-motor-controller.png"
  alt="REV Robotics SPARK MAX motor controller"
  caption="The SPARK MAX is a separate controller. Its configuration must match the connected brushed or brushless motor."
  sourceUrl="https://docs.revrobotics.com/brushless/spark-max/overview"
  sourceLabel="REV Robotics SPARK MAX documentation"
/>

### SPARK MAX Example

```java
package frc.robot;

import edu.wpi.first.wpilibj.TimedRobot;
import com.revrobotics.spark.SparkMax;
import com.revrobotics.spark.SparkLowLevel.MotorType;

public class Robot extends TimedRobot {
    // Create motor instance: CAN ID 1, Brushless motor type
    private SparkMax m_motor = new SparkMax(1, MotorType.kBrushless);
    
    @Override
    public void teleopPeriodic() {
        // Set motor to 50% speed forward (range: -1.0 to 1.0)
        m_motor.set(0.5);
    }
}
```

### Control Basics

**Speed:** Values range from -1.0 (full reverse) to 1.0 (full forward). 0.0 stops the motor.

**Multiple Motors:** Create separate instances for each motor (e.g., left and right drive motors).

### Multiple Motors

<CodeTabs tabs={[{"label":"Talon FX","code":"package frc.robot;\n\nimport edu.wpi.first.wpilibj.TimedRobot;\nimport com.ctre.phoenix6.hardware.TalonFX;\nimport com.ctre.phoenix6.controls.DutyCycleOut;\n\npublic class Robot extends TimedRobot {\n    // Create left and right motors with different CAN IDs\n    private TalonFX m_left = new TalonFX(1);\n    private TalonFX m_right = new TalonFX(2);\n    private DutyCycleOut m_output = new DutyCycleOut(0);\n    \n    @Override\n    public void teleopPeriodic() {\n        // Set both motors to 50% speed\n        m_left.setControl(m_output.withOutput(0.5));\n        m_right.setControl(m_output.withOutput(0.5));\n    }\n}","language":"java"},{"label":"SPARK MAX","code":"package frc.robot;\n\nimport edu.wpi.first.wpilibj.TimedRobot;\nimport com.revrobotics.spark.SparkMax;\nimport com.revrobotics.spark.SparkLowLevel.MotorType;\n\npublic class Robot extends TimedRobot {\n    // Create left and right motors with different CAN IDs\n    private SparkMax m_left = new SparkMax(1, MotorType.kBrushless);\n    private SparkMax m_right = new SparkMax(2, MotorType.kBrushless);\n    \n    @Override\n    public void teleopPeriodic() {\n        // Set both motors to 50% speed\n        m_left.set(0.5);\n        m_right.set(0.5);\n    }\n}","language":"java"}]} />

<RulesBox title="Key Points" subtitle="Remember:">

- Create motors as private fields.
- Use unique CAN IDs.
- Talon FX uses `setControl`.
- SPARK MAX uses `set`.
- Range is -1.0 to 1.0.

</RulesBox>

<Sources items={[
  {"source": "mantik", "path": "src/content/frc/frc-basics/intro-to-motors.mdx", "note": "The lesson's topic and original outline come from Mantik's page of the same name; the text was rewritten for GarageDocs."},
  {"source": "wpilib", "path": "docs/software/hardware-apis/motors/using-motor-controllers.html", "note": "Motor controllers in WPILib and the vendor libraries."},
  {"source": "ctre", "path": "docs/api-reference/api-usage/api-overview.html", "note": "Phoenix 6 API overview: devices, control requests, status signals."},
  {"source": "ctre", "path": "docs/api-reference/device-specific/talonfx/open-loop-requests.html", "note": "<code>DutyCycleOut</code> for the first motor."},
  {"source": "rev", "path": "brushless/spark-max/gs", "note": "SPARK MAX getting started."},
  {"source": "rev", "path": "revlib/", "note": "REVLib and <code>SparkMax</code>."},
  {"source": "rev", "path": "brushless/neo/v1.1", "note": "The NEO brushless motor."},
  {"source": "cobra", "path": "src/main/java/frc/robot/Robot.java", "note": "The <code>TimedRobot</code> lifecycle methods the examples fill in."}
]} />

<LinkGrid links={[{"label":"Controllers and Inputs","id":"controllers-and-inputs"},{"label":"CTRE Setup","id":"ctre-hardware-setup"},{"label":"REV Setup","id":"rev-hardware-setup"}]} title="Resources" section="frc" />
