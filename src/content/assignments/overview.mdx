---
title: Practice Assignments
lessonId: overview
section: assignments
isOverview: true
description: "A practice roadmap that pairs with the Java track: scenarios, rubrics, and hints that stop short of the answer."
---

A practice roadmap that pairs with [Java Training](/java). Every assignment names the
lessons it builds on, gives you a scenario and a rubric, and stops short of handing you a
solution. Hints help you get unstuck without giving away the work the assignment asks you
to practice.

These assignments are **standalone Java programs kept in one GitHub repository**. You do
not need WPILib installed for any of them. The shared repository keeps your work and
submission history together while each program remains independent. The point is to get
the language locked in before you also have to reason about robot hardware, the command
scheduler, and the 50 Hz periodic loop.

<RulesBox title="Read this page before you start">

- The rubric is yours to fill in honestly: nothing here scores you
- Hints never contain the working answer
- Write your own code; naming a technique you saw is fine, pasting a class is not
- **Java 25** is the standard. Install it locally, and every JavaDoc link here
  points at the Java 25 API
- The in-page editor runs **Java 17**: records, `var`, text blocks, arrow `switch`
  and sealed types all work. What it does *not* have is anything newer than 17;
  pattern matching in `switch`, for instance, arrived in Java 21
- The first Run on a page takes about half a minute while the compiler warms up.
  After that it answers in well under a second
- Complete [A00: Practice Repository Setup](/java/assignments/a00-practice-repository-setup)
  before starting A01. Push every finished assignment to that repository

</RulesBox>

### Submit your work through GitHub

[A00: Practice Repository Setup](/java/assignments/a00-practice-repository-setup) creates one
student-owned repository for the full Practice track. Every later assignment names a
folder in that repository. When an assignment is complete, update the repository's
submission table, commit the assignment folder, and push it to GitHub.

Submit the repository URL to your mentor or reviewer once. They can use the same link to
check later assignments, review commit history, and compare your self-score with the code.
A private repository works if the reviewer has access.

### The three tracks

Three tracks, in order. Each assumes you finished the one before it.

<ContentTable
  headers={["Track", "Assignments", "What it locks in"]}
  rows={[
    ["<a href=\"/java/assignments/a00-practice-repository-setup\">Track 1: Foundations</a>", "A00–A10", "GitHub submission setup, compiling and running, input and output, arithmetic, strings, conditionals, loops, methods, arrays"],
    ["<a href=\"/java/assignments/a11-simple-robot-class\">Track 2: Objects</a>", "A11–A22", "Classes, references, composition, enums, inheritance, interfaces, records, sorting, exceptions, file I/O, lambdas"],
    ["<a href=\"/java/assignments/a23-generic-ring-buffer\">Track 3: Collections</a>", "A23–A28", "Generics, ArrayList, Map, streams, recursion, reading complexity"]
  ]}
/>

Finishing Track 3 is enough to start [Kit Bot](/kit-bot). There is no standalone
capstone here. The multi-file, whole-program practice happens in the
[Kit Bot capstone](/kit-bot/rc-capstone). The "small interactive tool built from everything
you have learned" idea lives on in the shot-calibration table in
[R08: LauncherIO](/kit-bot/r08-launcher-io), where it becomes part of the robot's code.

### What an assignment looks like

Every assignment uses the same shape, so you always know where to look:

1. **Topic**: a few words naming the concept this targets
2. **Cites**: links into [Java Training](/java). Not optional reading; they define the
   vocabulary the rubric expects
3. **Difficulty and time**: a rough estimate for someone who read the cited lessons and
   is not stuck looking up syntax
4. **Scenario**: the problem in plain language
5. **What to build**: the concrete requirements: method names, expected input and output,
   edge cases. This is the contract you are filling
6. **Hints**: collapsed, for when you are stuck. Method signatures, a one-line skeleton,
   or a pointer to the exact JavaDoc page
7. **Repository checkpoint**: the folder, checklist update, commit, and push that submit
   the work
8. **Self-grading rubric**: a point breakdown totalling 100
9. **Stretch goals**: optional extensions if you finish early
10. **References**: JavaDoc, background reading, related lessons

### The rubric

Every assignment scores the same four sections. Per-line points shift with the
assignment; the totals do not.

<ContentTable
  headers={["Section", "Points", "What earns the points"]}
  rows={[
    ["Functionality", "60", "Right output for the example inputs and common boundary cases. Compiles cleanly. No crashes on valid input."],
    ["Edge cases", "15", "Empty inputs, boundary values, malformed lines, division by zero, missing files. Each assignment lists the ones it cares about."],
    ["Code quality", "20", "Meaningful names. Each method does one thing. No copy-pasted code that could be a helper. <code>final</code> on what does not change. Fields <code>private</code>. No dead code."],
    ["Comments", "5", "Author and date at the top. A short note on any decision whose reason may not be clear: <em>why</em>, not a play-by-play of what the code does."],
    ["Total", "100", ""]
  ]}
/>

If you cannot honestly give yourself the full 60 in Functionality, you are not done. Edge
cases and code quality come *after* the program works.

### Hints are not solutions

A hint is allowed to be a signature with an empty body, a pointer to the JavaDoc page that
has what you need, or a note that the classic mistake here is comparing two `String`
values with `==`.

A hint is **not** the worked answer with one variable renamed. If a hint looks like the
program with three lines removed, the problem is too hard for this stage or the hint is
too generous: the prompt gets reworked, not the hint.

### Build your own; don't paste

The FRC programs that win competitions are written by students who understand every line.
This track holds that standard.

You will hear about production codebases: your team's robot code, AdvantageKit, public
repositories from other teams. Those are valuable as references. **Naming a technique you
saw is fine. Pasting a class from someone else's repository into your own project is
not.**

When an assignment asks you to design a small version of a technique, that is deliberate.
The smaller version asks you to make the design decisions yourself, including which fields belong
on the class, what methods the interface needs, what gets validated in the constructor.
Once you have earned that judgment, reading a 500-line production version teaches you
something. Before you have earned it, copying it teaches you nothing.

A practical rule: **if you cannot explain a line you wrote, rewrite it until you can.**

### Pitfalls that bite almost everyone

- **Comparing strings with `==`**: use `.equals()`. See [Comparing strings](/java/fundamentals/java-conditionals#comparing-strings)
- **Mixing `nextInt()` and `nextLine()`**: a stray newline stays in the buffer
- **Integer division**: `7 / 2` is `3`, not `3.5`. See [Integer vs floating-point division](/java/fundamentals/java-calculations#integer-vs-floating-point-division)
- **Off-by-one in array loops**: use `< array.length`, not `<=`. See [Arrays](/java/fundamentals/java-arrays)
- **`NullPointerException` from an object array**: new object arrays start full of `null`
- **Forgetting `@Override`**: the compiler catches typos and signature mismatches for you
- **`catch (Exception e) {}`** that hides bugs: catch specifics, or let it propagate

<RulesBox title="Origin">

These assignments were written by FRC 4451's mentors for this site, with direct inspiration
from the University of South Carolina's [CSCE 145](https://www.cse.sc.edu/~shephejj/csce145/)
and [CSCE 146](https://www.cse.sc.edu/~shephejj/csce146/), and built to run in the browser
with hidden checks.

</RulesBox>
