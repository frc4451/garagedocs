---
title: "A01: Hello, Team"
lessonId: a01-hello-team
section: assignments
group: foundations
groupLabel: "Track 1: Foundations"
groupOrder: 1
order: 1
difficulty: beginner
duration: 20 min
description: "Confirm your setup compiles and runs ordinary Java, and print a four-line introduction."
---

**Topic:** first compile and run, `System.out.println`, comments.
**Cites:** [Java Setup](/java/fundamentals/java-setup), [Printing](/java/fundamentals/java-printing).
**Difficulty:** beginner. **Estimated time:** 20 minutes.

**Repository checkpoint:** Work in `assignments/a01-hello-team/` inside the repository from [A00](/java/assignments/a00-practice-repository-setup). Keep every source and input file for
this assignment there. After completing the checks and rubric, update the root
`README.md`, commit with `Complete A01: Hello, Team`, and push to GitHub.

### Scenario

Write and run a Java program that introduces you to a reviewer. This checks the local tools and the repository workflow before you add calculations or input.

### What to build

A single file named `HelloTeam.java` with a class `HelloTeam` containing a `main` method.

Running it prints **exactly** four lines, in this order:

```text
Name: <your name here>
Team: <your team number and name here>
Role: <programming / mechanical / electrical / strategy / etc.>
Ready to build.
```

Replace the angle-bracketed placeholders with your real information. The final line,
`Ready to build.`, is fixed.

### Requirements

- A header comment at the top of the file: your name, the date, and one sentence on what
  the program does
- A `class` declaration whose name matches the filename
- Exactly one `main` method
- Exactly four `println` calls in `main`

<JavaPlayground id="a01-hello-team" />

<RulesBox title="Hints" items={[
  "<strong>Stuck on the file structure?</strong> Every Java file needs a <code>class</code> block, and a runnable program needs a <code>main</code> method inside it. The shape is fixed; only the class name and the body of <code>main</code> change between programs. See <a href=\"/java/fundamentals/java-printing\">Printing</a> for the token-by-token breakdown.",
  "<strong>Will not compile?</strong> Check three things first: the filename matches the public class name exactly including capitalization, every statement ends with a semicolon, and every <code>{</code> has a matching <code>}</code>.",
  "<strong>Everything on one line?</strong> <code>System.out.print(...)</code> does not add a newline; <code>System.out.println(...)</code> does. This assignment wants <code>println</code> for every line."
]} />

### Self-grading rubric

<ContentTable
  headers={["Section", "Points", "What earns the points"]}
  rows={[
    ["Functionality", "60", "Compiles and runs. Prints exactly four lines, in order, with the trailing <code>Ready to build.</code> exactly as shown."],
    ["Edge cases", "15", "The placeholders are filled in: no literal <code>&lt;your name here&gt;</code> left in the output. No extra blank lines before or after."],
    ["Code quality", "20", "Filename matches the class name. The class is <code>HelloTeam</code>, not <code>helloteam</code> or <code>Hello_Team</code>. The header comment exists and is meaningful."],
    ["Comments", "5", "A real header comment naming you, the date, and what the program does."],
    ["Total", "100", ""]
  ]}
/>

The two automated checks above cover the shape of the output and the placeholders. Points
for code quality and comments are yours to award honestly: nothing on this page can see
whether your header comment is meaningful.

### Stretch goals

- Add a fifth line built with string concatenation: `Today is ` + the year, e.g.
  `Today is 2026.` Hard-code the year; you have not met `java.time` yet.
- Re-implement the four lines using exactly **one** `println` and the `\n` escape
  sequence. Decide which version you would rather maintain when "Role" becomes
  "Subsystem".

### References

- JavaDoc: [`java.lang.System`](https://docs.oracle.com/en/java/javase/25/docs/api/java.base/java/lang/System.html)
- JavaDoc: [`java.io.PrintStream`](https://docs.oracle.com/en/java/javase/25/docs/api/java.base/java/io/PrintStream.html): the type of `System.out`, where `println` lives
- Wikipedia: [Hello, World! program](https://en.wikipedia.org/wiki/%22Hello,_World!%22_program)

<Sources items={[
  {"source": "csce145", "note": "The first-program exercise shape."},
  {"source": "dev-java", "path": "getting-started/", "note": "Compiling and running a first program."},
  {"source": "java-api", "path": "java.base/java/lang/System.html", "note": "<code>System.out</code>."},
  {"url": "https://docs.oracle.com/en/java/javase/25/docs/specs/man/javac.html", "label": "javac reference", "note": "Compiling from the command line."},
  {"url": "/java/fundamentals/java-setup", "label": "Setting Up Java", "note": "Java 25 install, javac/java from the command line, feeding input from a file."}
]} />

