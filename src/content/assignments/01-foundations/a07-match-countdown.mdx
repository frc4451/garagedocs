---
title: "A07: Match Countdown"
lessonId: a07-match-countdown
section: assignments
group: foundations
groupLabel: "Track 1: Foundations"
groupOrder: 1
order: 7
difficulty: intermediate
duration: 60 min
description: "Print a 151-line match timeline with a text progress bar, then offer to run it again."
---

**Topic:** loops (`for`, `while`, `do-while`), formatted output, nested loops.
**Cites:** [For Loops](/java/fundamentals/java-for-loops), [While Loops](/java/fundamentals/java-while-loops), [Loop Control](/java/fundamentals/java-loop-control).
**Difficulty:** intermediate. **Estimated time:** 60 minutes.

**Repository checkpoint:** Work in `assignments/a07-match-countdown/` inside the repository from [A00](/java/assignments/a00-practice-repository-setup). Keep every source and input file for
this assignment there. After completing the checks and rubric, update the root
`README.md`, commit with `Complete A07: Match Countdown`, and push to GitHub.

### Scenario

Before you put a countdown on the driver station you want to see the whole match written
out, second by second, and check that the phase boundaries land where you think they do. A
text progress bar makes an off-by-one error visible in a way that a number never does.

### What to build

A single file `MatchCountdown.java`. No user input for the timeline itself. Print **151
lines**, one for each `t = 0` through `t = 150`:

```text
t=  0  AUTO     [                                ]   0%
t=  1  AUTO     [                                ]   0%
t= 15  TELEOP   [###                             ]  10%
t= 75  TELEOP   [################                ]  50%
t=120  ENDGAME  [#########################       ]  80%
t=150  OVER     [################################] 100%
```

After all 151 lines, prompt `Run again? (y/n):` and replay on `y`. Use a `do-while` for the
outer replay loop.

### Format details

- The bar is **32 characters** between the brackets. Fill `t * 32 / 150` of them with `#`
  using integer division, and pad the rest with spaces
- The percent is the integer percent elapsed, `t * 100 / 150`, right-aligned in three columns
- Phases use the [A05](/java/assignments/a05-match-phase-detector) boundaries: `AUTO` below 15,
  `TELEOP` from 15 to 119, `ENDGAME` from 120 to 149, `OVER` at 150


<RulesBox title="Plan it as sessions" items={[
  "<strong>Session 1.</strong> Get the file compiling with every method stubbed, then make these pass: <em>Start, middle and end of the match</em>, <em>Endgame boundary and the last second before it ends</em>.",
  "<strong>Session 2.</strong> Finish the edge cases and the rubric items nothing checks: <em>The bar never overflows its brackets</em>."
]} />

<JavaPlayground id="a07-match-countdown" />

<RulesBox title="Hints" items={[
  "<strong>Integer division is the feature here, not the bug.</strong> <code>t * 32 / 150</code> in <code>int</code> arithmetic truncates, which is exactly what you want for a bar that only grows in whole blocks. Multiply before you divide: <code>t / 150 * 32</code> is zero for almost every value of t.",
  "<strong>Building the bar.</strong> A nested loop over the 32 positions, appending <code>#</code> below the fill count and a space at or above it. Or build it in one line with <code>String.repeat</code> on each half: the editor runs Java 17, so that is available.",
  "<strong>Column alignment.</strong> <code>%3d</code> right-aligns a number in three columns and <code>%-8s</code> left-aligns text in eight. Those two plus the literal brackets reproduce the layout above exactly.",
  "<strong>Why <code>do-while</code>.</strong> The timeline always prints at least once, and only then do you ask whether to repeat. That is the shape <code>do-while</code> exists for: a <code>while</code> loop would make you ask before there is anything to repeat."
]} />

### Self-grading rubric

<ContentTable
  headers={["Section", "Points", "What earns the points"]}
  rows={[
    ["Functionality", "60", "Exactly 151 lines. The bar, the percent and the phase are right for every second. The replay prompt appears after the timeline and repeats on y."],
    ["Edge cases", "15", "<code>t = 0</code> is an empty bar at 0%. <code>t = 150</code> is a full bar at 100%. The bar never exceeds 32 characters and never spills past the closing bracket."],
    ["Code quality", "20", "The bar width and the match length are named constants. The phase decision is one helper, not repeated inline. No copy-pasted format strings."],
    ["Comments", "5", "Author header, and a line explaining the integer-division bar formula for the next reader."],
    ["Total", "100", ""]
  ]}
/>

### Stretch goals

- Take the match length and bar width as inputs and confirm the output still lines up for a
  105-second match.
- Mark the phase transitions with a different character so `AUTO` to `TELEOP` is visible at a
  glance in the bar itself.

### References

- JavaDoc: [`java.util.Formatter`](https://docs.oracle.com/en/java/javase/25/docs/api/java.base/java/util/Formatter.html): width, alignment and precision
- JavaDoc: [`java.lang.String.repeat`](https://docs.oracle.com/en/java/javase/25/docs/api/java.base/java/lang/String.html#repeat(int))
- Wikipedia: [Progress bar](https://en.wikipedia.org/wiki/Progress_bar)

<Sources items={[
  {"source": "csce145", "note": "Loops."},
  {"source": "dev-java", "path": "language-basics/controlling-flow/", "note": "while and for loops."},
  {"source": "dev-java", "path": "numbers-strings/strings/", "note": "Building output strings."}
]} />
