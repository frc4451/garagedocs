---
title: "A02: Battery Sanity Check"
lessonId: a02-battery-sanity
section: assignments
group: foundations
groupLabel: "Track 1 — Foundations"
groupOrder: 1
order: 2
difficulty: beginner
duration: 30–45 min
description: Predict the voltage at the motor terminals under load, with formatted output and named thresholds.
---

**Topic:** primitive types, arithmetic, integer division, formatted output, `final` constants.
**Cites:** [Java Variables](/java/java-variables), [Calculating with Numbers](/java/java-calculations).
**Difficulty:** easy. **Estimated time:** 30–45 minutes.

### Scenario

Before a match your team checks the battery's resting voltage, the total resistance through
the cables and connectors, and the worst-case current the drivetrain will pull at stall.
From those three numbers you can predict the voltage at the motor terminals under load:

```text
vAtMotor = vRest - (current x resistance)
```

You want a console utility that does the arithmetic, so nobody reaches for a calculator at
every pre-match check.

### What to build

A single file `BatterySanity.java` with one class and one `main`. It should:

1. Read **three** doubles in this order — resting voltage in volts, total resistance in
   ohms, worst-case current in amps
2. Compute `vAtMotor`
3. Print the three inputs and the result, all to **two decimal places**
4. Print **one** verdict line:

<ContentTable
  headers={["Predicted voltage", "Verdict"]}
  rows={[
    ["9.0 V or more", "<code>Healthy.</code>"],
    ["7.0 V up to but not including 9.0 V", "<code>Marginal, consider a fresh battery.</code>"],
    ["Below 7.0 V", "<code>Replace immediately.</code>"]
  ]}
/>

Worked example: resting 12.6 V, resistance 0.025 ohm, current 150 A predicts 8.85 V, which
is marginal.

### Requirements

- Use `double` for every number. `int` is wrong here — see the hint on division
- Use `printf` or `String.format`. Two decimal places everywhere
- Define the two thresholds as `final double` constants. No bare numbers in the `if` chain
- Exactly one `Scanner`, closed before the program exits


<JavaPlayground id="a02-battery-sanity" />

<RulesBox title="Hints" items={[
  "<strong>Tempted by <code>int</code> because the inputs look whole?</strong> Current reads as <code>150</code> in the example, but store it as an <code>int</code> and any later division silently throws away the fractional part. Default to <code>double</code> for anything you will multiply or divide. See <a href=\"/java/java-calculations\">Calculating with Numbers</a>.",
  "<strong>Reading three numbers.</strong> <code>Scanner</code> has <code>nextDouble()</code>. Call it three times; no loop needed. All three reads are numeric, so you sidestep the leftover-newline gotcha entirely.",
  "<strong>Two decimals.</strong> <code>System.out.printf(\"%.2f%n\", value)</code> — <code>%.2f</code> is a decimal with two places, <code>%n</code> is a newline. <code>%8.2f</code> right-aligns in eight columns if you want the numbers to line up.",
  "<strong>Picking the branch.</strong> Three buckets, one <code>if / else if / else</code> chain. Order matters: write the highest threshold first."
]} />

### Self-grading rubric

<ContentTable
  headers={["Section", "Points", "What earns the points"]}
  rows={[
    ["Functionality", "60", "Reads three doubles in the right order, applies the formula, prints all three inputs and the prediction, and picks exactly one verdict."],
    ["Edge cases", "15", "A resistance of 0 gives a finite answer. A current of 0 returns the resting voltage. The boundary values 7.0 and 9.0 land in the right bucket — work out on paper which one before you run it."],
    ["Code quality", "20", "<code>final</code> constants for both thresholds. Meaningful names. No copy-pasted printing; pull out a template if you repeat one."],
    ["Comments", "5", "Author header, and a line near the formula naming the assumption you are making."],
    ["Total", "100", ""]
  ]}
/>

### Stretch goals

- Add a fourth prompt for the **expected** current draw, not just stall. Print two
  predictions and pick the verdict from the worse of the two.
- Reject negative voltage, resistance or current with a clear message. That is a preview of
  [Try/Catch](/java/java-try-catch#why-use-trycatch), which gives you cleaner tools for it.

### References

- JavaDoc: [`java.util.Scanner`](https://docs.oracle.com/en/java/javase/25/docs/api/java.base/java/util/Scanner.html)
- JavaDoc: [`java.util.Formatter`](https://docs.oracle.com/en/java/javase/25/docs/api/java.base/java/util/Formatter.html) — the full grammar for `%.2f`, `%8.2f` and `%n`
- Wikipedia: [Ohm's law](https://en.wikipedia.org/wiki/Ohm%27s_law)

<Sources items={[
  {"source": "csce145", "note": "Arithmetic and formatted output exercises."},
  {"source": "dev-java", "path": "language-basics/primitive-types/", "note": "Primitive types and integer division."},
  {"source": "jls", "path": "jls-15.html", "note": "Integer division and operators (chapter 15)."},
  {"source": "java-api", "path": "java.base/java/util/Formatter.html", "note": "<code>printf</code> / <code>String.format</code> syntax."},
  {"url": "/java/java-setup", "label": "Setting Up Java", "note": "Java 25 install, javac/java from the command line, feeding input from a file."}
]} />
