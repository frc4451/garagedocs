---
title: "R09: Characterization, Part 2: Calculate kS and kV"
lessonId: r09-characterization-part-2
section: kit-bot
group: io-pattern
groupLabel: "Phase 3: Hardware via IO"
groupOrder: 3
order: 9.5
duration: 35 min
description: Work through the line-fit math and check what the calculated feedforward gains mean.
durationFixed: true
---


Part 1 recorded pairs of steady velocity and applied voltage. Part 2 explains how the code fits one line through those points.

```text
y = kS + kV × x

x = velocity
y = voltage
```

On a graph with velocity across the bottom and voltage up the side:

- `kS` is the y-intercept. It is the modeled voltage at the point where motion begins.
- `kV` is the slope. It says how many additional volts the model needs for each unit of velocity.

### Start with two points

If you had only two settled samples, you could calculate the slope and intercept directly:

```text
kV = (voltage2 - voltage1) / (velocity2 - velocity1)
kS = voltage1 - kV × velocity1
```

Real measurements contain noise, so choosing a different pair can change the result. Part 1 collected several points and used **linear regression**, which fits one line across all accepted samples.

### Build the four sums

For every accepted sample, the loop adds:

```java
sumX += velocity;
sumY += voltage;
sumXY += velocity * voltage;
sumX2 += velocity * velocity;
```

The variable names match the algebra:

| Code | Mathematical sum | Meaning |
|---|---|---|
| `sumX` | `Σx` | All measured velocities added together |
| `sumY` | `Σy` | All applied voltages added together |
| `sumXY` | `Σxy` | Each velocity multiplied by its voltage, then added |
| `sumX2` | `Σx²` | Each velocity squared, then added |
| `n` | `n` | Number of accepted samples |

The common denominator is:

```text
denominator = n × Σx² - (Σx)²
```

Then the intercept and slope are:

```text
kS = (Σy × Σx² - Σx × Σxy) / denominator
kV = (n × Σxy - Σx × Σy) / denominator
```

These are the same equations written in `printResults`. The code is longer because each mathematical sum needs a variable and a loop.

### Work one example by hand

The values below are an arithmetic example, not Kit Bot measurements.

| Velocity (rot/s) | Voltage (V) | velocity × voltage | velocity² |
|---:|---:|---:|---:|
| 1.0 | 2.0 | 2.0 | 1.0 |
| 2.0 | 3.5 | 7.0 | 4.0 |
| 3.0 | 5.0 | 15.0 | 9.0 |
| **Sum** | **10.5** | **24.0** | **14.0** |

For these three points:

```text
n = 3
Σx = 6.0
Σy = 10.5
Σxy = 24.0
Σx² = 14.0

denominator = 3 × 14.0 - 6.0² = 6.0
kS = (10.5 × 14.0 - 6.0 × 24.0) / 6.0 = 0.5 V
kV = (3 × 24.0 - 6.0 × 10.5) / 6.0 = 1.5 V/(rot/s)
```

The fitted model is:

```text
voltage = 0.5 + 1.5 × velocity
```

At `2.0 rot/s`, the model predicts `0.5 + 1.5 × 2.0 = 3.5 V`, which matches the middle example point.

### Check your own output

Use two or three samples from one Part 1 test:

1. Write the samples in a table with the same four columns.
2. Add each column.
3. Substitute the sums into the equations.
4. Compare your result with the Java output.

Small differences can come from rounding the printed samples. A large difference usually means a copied value, multiplication, or sum is wrong.

### Keep the units attached

The drivetrain and launcher use the same math but different velocity units:

| Mechanism | Velocity unit | `kS` unit | `kV` unit |
|---|---|---|---|
| Drivetrain | meters per second | volts | `V/(m/s)` |
| Launcher | rotations per second | volts | `V/(rot/s)` |

Do not copy a drivetrain `kV` into the launcher. The numbers describe different mechanisms and different velocity units.

### Why there is no kA in this exercise

The full simple-motor model can include acceleration:

```text
voltage = kS × sign(velocity) + kV × velocity + kA × acceleration
```

Part 1 waited for each voltage step to settle before recording. At that moment, acceleration is approximately zero, so `kA × acceleration` is approximately zero. This test cannot measure `kA`, and the Kit Bot lessons do not claim that it does.

### Use the result later

WPILib's `SimpleMotorFeedforward` uses the same equation. Supply gains measured in the same velocity unit that the caller will use:

```java
SimpleMotorFeedforward feedforward =
    new SimpleMotorFeedforward(kS, kV);

double requestedVolts =
    feedforward.calculate(targetVelocity);
```

For negative target velocity, `SimpleMotorFeedforward` applies `kS` in the negative direction. Feedback control and precise velocity targets come later in the FRC controls section. This lesson's goal is to understand and verify the two-term model.

### Record the result with its context

Store each accepted result with:

- Mechanism name
- Velocity unit
- Simulation or real hardware
- Test date
- Voltage steps and settle time
- Hardware configuration, including gearing and wheel or roller size

Simulation gains describe the simulation model. Use real-hardware gains only after a mentor-approved hardware test.

<RulesBox title="Done when">

You can calculate `kS` and `kV` from a sample table, explain why settled samples remove the acceleration term, and state the units for both mechanisms. Your saved results identify the mechanism, units, environment, voltage steps, and settle time.

</RulesBox>

<Sources items={[
  {"source": "cobra", "path": "src/main/java/org/robotzgarage/frc2026/subsystems/rollers/single/SingleRoller.java", "note": "The explicit <code>kS</code> and <code>kV</code> regression used by the team roller library."},
  {"url": "https://github.com/Mechanical-Advantage/RobotCode2023/blob/main/src/main/java/org/littletonrobotics/frc2023/commands/FeedForwardCharacterization.java", "label": "Mechanical Advantage FeedForwardCharacterization", "note": "The reusable characterization and first-order regression pattern."},
  {"source": "wpilib", "path": "docs/software/advanced-controls/introduction/introduction-to-feedforward.html", "note": "The simple-motor feedforward equation and gain meanings."},
  {"source": "wpilib-javadoc", "path": "edu/wpi/first/math/controller/SimpleMotorFeedforward.html", "note": "Applying <code>kS</code> and <code>kV</code> to a requested velocity."}
]} />
