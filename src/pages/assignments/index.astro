---
import { withBase } from '@/lib/url';
const destination = withBase('/java/assignments');
void destination; // Astro's checker does not count references in a top-level redirect return.
return Astro.redirect(destination, 301);
---
