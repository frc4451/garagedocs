---
import LessonLayout from '@/layouts/LessonLayout.astro';
import { getOverview, getJavaSidebarGroups } from '@/lib/content';
import { render } from 'astro:content';
import { components } from '@/mdx-components';

const overview = await getOverview('assignments');
if (!overview) throw new Error('Practice Assignments overview not found');

const sidebarGroups = await getJavaSidebarGroups();
const { Content } = await render(overview);
---

<LessonLayout
  title={overview.data.title}
  description={overview.data.description}
  currentPath="/java/assignments"
  sidebarGroups={sidebarGroups}
  javaExamples={true}
>
  <Content components={components} />
</LessonLayout>
